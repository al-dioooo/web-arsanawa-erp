// Canonical status rendering lives in the shared UI layer so every module maps
// the same status to the same tone and label.
export { StatusBadge, statusTone, formatStatusLabel, type StatusTone } from "@/components/ui/status-badge"
